
#include "sea.h"

sea_tokens sea_tokenize(const char *source) {
    sea_tokens out;

    size_t line = 1, column = 1, index = 0;
    size_t start;
    size_t slen = strlen(source);

    return out;
}
